# Walking skeleton: `verify`-labelled drain produces `flow: "verify"` in run log

## What

A ticket labelled `verify` in Linear drains through drain-cycle with `flow: "verify"` recorded in the run log entry; all other ticket drains continue to produce `flow: null`.

## Why

This is the foundation that all five new roles write to and the grading commands read from. No other milestone can proceed without the system distinguishing which drains use the new flow.

## Assumptions

* `labels.py` already handles `repo:` and `model:` parsing with an extensible pattern.
* The run-log format can accommodate a new top-level string field without breaking existing readers.

## Key Risks

* A `verify` label on a ticket that also has ambiguous or conflicting labels must not silently resolve — existing label-resolution error handling applies.
* Existing run-log readers must not break on entries that gain a new field.

## Definition of Done

* Draining a `verify`-labelled ticket produces a run-log entry with `flow: "verify"`.
* Draining an unlabelled ticket produces `flow: null`.
* All existing tests pass.
* The `verify` label does not interfere with `repo:` or `model:` label resolution.

---

Execution instructions:
- Working directory: /Users/anton/src/drain-cycle/.worktrees/ABA-320
- Base branch: main
- Completion sequence for issue ABA-320 (run in this order, before marking Done):
  1. Run `/code-review-and-quality` against the working-tree changes.
  2. Fix any Critical or Required findings. Lower-severity findings are at your discretion.
  3. Commit and push to main.
  4. Post a short review-summary comment on the Linear issue via `mcp__claude_ai_Linear__save_comment` (count of findings by severity, fixed vs deferred).
  5. Transition issue to Done via `mcp__claude_ai_Linear__save_issue` (state: "Done").

before marking Done: run /code-review-and-quality on the working-tree changes, fix Critical/Required findings, commit + push, then post a review-summary comment on the issue and transition to Done.